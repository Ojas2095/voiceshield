# AI/ML Pod — Python Dependencies

# Core ML
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.35.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Visualization & Grad-CAM
matplotlib>=3.8.0

# Dataset & training
datasets>=2.14.0

# Evaluation metrics
scikit-learn>=1.3.0

# TTS for fake audio generation (friend's task)
gTTS>=2.4.0
# TTS>=0.22.0  # Uncomment for Coqui XTTS (requires GPU, large install)

# Layer 2 ASR (optional — enable ENABLE_LAYER2_ASR). Falls back gracefully if absent.
# faster-whisper>=1.0.0
